<h2> SG Blog </h2>

<ul>
  <% @posts.each do |post| %>
    <li class="post-title"><%= link_to post.title,  post_path(post.id) %></li>
  <% end %>

</ul>
<div style=" display:inline-block"><%= button_to "New Post", new_post_path,  method:"get" %></div>
